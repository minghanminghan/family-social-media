// Number of posts fetched per page for the home feed and search results
// infinite scroll (components/InfiniteScroll.tsx).
export const PAGE_SIZE = 5

// Rows fetched per page by the /media gallery (components/MediaGallery.tsx).
// Larger than PAGE_SIZE because a page there is a grid of thumbnails / list
// rows rather than full post cards.
export const GALLERY_PAGE_SIZE = 24
